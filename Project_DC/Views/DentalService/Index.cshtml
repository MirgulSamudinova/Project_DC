@model IEnumerable<Project_DC.Models.DentalService>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameOfService)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DentalServiceGroupId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsToothService)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameOfService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item._DentalServiceGroup.DentalServiceGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsToothService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
